Emoji bot - a Discord bot that converts basic text to Discord emojis & does other standard bot functions.

dependencies:
  node.js, 
  discord.js,
  nodemon, 
  dotenv,
  
set token and prefix in a .env file

to run:
  cd to root folder in CMD.
  type: nodemon index.js
